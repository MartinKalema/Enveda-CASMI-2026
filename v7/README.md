# v7: floor refinements (tight window, entropy-weighted cosine, formula-mass index)

Plain English: stop polishing fills (all 0.093-0.095) and rebuild the floor
that earns every point so far. Survey recipes: +-10ppm uncapped windows,
entropy-weighted cosine, formula-mass indexing with hybrid tolerance
max(10ppm, 0.01Da). Validated BOTH retrieval-style (spectra held out, truth
in pool - matches hidden mixed regime) and disjoint (novels).
